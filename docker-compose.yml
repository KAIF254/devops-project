version: '3'

services:

  backend:
    build: ./backend
    ports:
      - "5000:5000"

  frontend:
    image: nginx:latest
    ports:
      - "3000:80"
    volumes:
      - ./frontend:/usr/share/nginx/html